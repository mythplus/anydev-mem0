# 2026-03-20 工作日志

## OpenMemory 前端优化（第一轮）
完成了 memories 页面核心组件的性能优化：
- **MemoriesSection**: skeleton-to-overlay loading 模式，fetchIdRef 竞态控制
- **MemoryFilters**: debounce 用 useMemo 重写，350ms，cleanup on unmount
- **MemoryTable**: per-item operatingIds Set 替代全局 isLoading
- **FilterComponent**: 移除直接 fetchMemories 调用，改为只更新 Redux state；懒加载 apps/categories
- **useMemoriesApi**: 所有函数 useCallback，useRef 防 stale closure，functional setState
- **animation.css**: GPU 加速（translate3d），缩短动画时间

## OpenMemory 前端优化（第二轮）
- **memory/[id]/page.tsx**: 用本地 localLoading/localError 替代共享 isLoading，修复子组件 API 调用干扰页面 loading 的问题
- **MemoryDetails.tsx**: 移除冗余 fetchMemoryById 调用，添加 hasUpdates 监听自动刷新
- **MemoryActions.tsx**: 使用本地 isOperating 替代共享 isLoading，useCallback 包装事件处理
- **AccessLog.tsx / RelatedMemories.tsx**: 添加正确的 useEffect 依赖项
- **update-memory.tsx**: 用 triggerRefresh 替代直接 fetchMemories 调用，独立 isUpdating 状态
- **Navbar.tsx**: useMemo 缓存路由映射，useCallback 包装 handleRefresh，刷新按钮旋转动画
- **Stats (useStats.ts)**: fetchStats 包装 useCallback
- **useConfig.ts**: 所有函数（fetchConfig/saveConfig/resetConfig/saveLLMConfig/saveEmbedderConfig）包装 useCallback
- **AppGrid.tsx**: isInitialLoad/isFetching 分离模式，loading overlay
- **AppFilters.tsx**: debounce 改用 useMemo，移除 isLoading skeleton，添加 cleanup
- **animation.css**: 新增 loading-overlay 淡入动画，toast 滑入动画

### 构建验证
- TypeScript 编译通过 (✓ Compiled successfully)
- 预渲染 /_not-found 失败是项目预先存在的问题（useLanguage in NotFound without SSR provider）
- 所有修改文件 lint 0 error
